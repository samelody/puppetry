apply from: '../gradle/lib.gradle'

dependencies {
    compile fileTree(dir: 'libs', include: ['*.jar'])
    compile libs.stathod
    compile libs.timber

    testCompile libs.junit
    testCompile libs.mockito

    debugCompile libs.leakcanary
    releaseCompile libs.leakcanaryStub
}